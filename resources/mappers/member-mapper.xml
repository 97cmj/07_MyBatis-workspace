<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberMapper">
<!--  
	namespace : 해당 mapper 파일의 고유한 별칭
	=> 실행하고자 하는 쿼리문을 찾아갈 때 써야하는 주소지 (중복 X)
 -->
 
 	<!--  
 		* DML 문 (INSERT, UPDATE, DELETE) 일 경우
 		<insert id="각sql문의 식별자" parameterType="전달받을자바타입(풀클래스명)">
 			실행하고자 하는 쿼리문 작성
 		</insert>
 		 <update></update>
 		 <delete></delete>
 		 
 		 => parameterType 은 완성된 쿼리문이면 (즉, 전달받은 값이 없다면) 생략 가능
 		 => update, delete 태그도 마찬가지
 		 => 미완성된 쿼리문의 경우
 		 pstmt.setXXX(n, xxx); 대신 #{필드명} 으로 표기하여 구멍을 메꿔주는 효과를 줄 수 있다.
 		 #{필드명} => 전달값.get필드명(); 가 내부적으로 호출되는 꼴, #은 PreparedStatement 를 의미함
 		 (문자열의 경우 양쪽에 홑따옴표가 붙어서 들어감) 
 	 -->
 	 
 	 <insert id="insertMember" parameterType="member">
 	 	INSERT INTO MEMBER(USER_NO
 	 					  ,USER_ID
 	 					  ,USER_PWD
 	 					  ,USER_NAME
 	 					  ,EMAIL
 	 					  ,BIRTHDAY
 	 					  ,GENDER
 	 					  ,PHONE
 	 					  ,ADDRESS)
 	 				VALUES(SEQ_UNO.NEXTVAL
 	 					  ,#{userId}
 	 					  ,#{userPwd}
 	 					  ,#{userName}
 	 					  ,#{email}
 	 					  ,#{birthday}
 	 					  ,#{gender}
 	 					  ,#{phone}
 	 					  ,#{address})
 	 </insert>
 	
 	
</mapper>
